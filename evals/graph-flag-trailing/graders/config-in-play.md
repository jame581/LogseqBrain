---
type: regex
target: trace
pattern: 'graph-source: config'
---
The sandboxed run can read the scaffold-written user config, so the decoy graph really was the
fallback a misparsed --graph would have used. Without this line the other graders prove nothing.
